pre-commit:
  commands:
    validate-placeholders:
      run: node scripts/validate-placeholders.js

    format-root:
      glob: '{,**/}*.{ts,tsx,js,jsx,mjs,cjs,json,md,css,yml,yaml}'
      exclude: '(^frontend/|^backend/|node_modules|\.next|\.git|pnpm-lock\.yaml)'
      run: pnpm exec prettier --ignore-unknown --write {staged_files}
      stage_fixed: true

    format-frontend:
      root: 'frontend/'
      glob: '{,**/}*.{ts,tsx,js,jsx,mjs,cjs,json,md,css,yml,yaml}'
      exclude: '(node_modules|\.next|\.git|pnpm-lock\.yaml)'
      run: pnpm exec prettier --ignore-unknown --write {staged_files}
      stage_fixed: true

    format-backend:
      root: 'backend/'
      glob: '{,**/}*.{ts,tsx,js,jsx,mjs,cjs,json,md,css,yml,yaml}'
      exclude: '(node_modules|\.next|\.git|pnpm-lock\.yaml)'
      run: pnpm exec prettier --ignore-unknown --write {staged_files}
      stage_fixed: true

    lint-frontend:
      root: 'frontend/'
      glob: 'src/**/*.{ts,tsx}'
      run: npx eslint --fix {staged_files}

    lint-backend:
      root: 'backend/'
      glob: 'src/**/*.ts'
      run: npx eslint --fix {staged_files}

commit-msg:
  commands:
    conventional-commit:
      run: node scripts/check-commit-msg.js {1}
